image: node:latest
stage: deploy

pages:
  before_script:
    - yarn
  script:
    - yarn prod
    - pwd
    - ls
    - echo "I am on gitlab-runner"
    - cp -rp dist public_html
  artifacts:
    paths:
      - public_html
  only:
    - master

pages:
  stage: deploy
  image: node:latest
  before_script:
    - yarn
  script:
    - yarn prod
    - pwd
    - echo "I am on gitlab-runner"
    - cp -rp dist public_html
  artifacts:
    paths:
      - public_html
  only:
    - master
